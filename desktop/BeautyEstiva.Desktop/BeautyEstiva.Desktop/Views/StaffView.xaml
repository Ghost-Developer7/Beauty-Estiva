<UserControl x:Class="BeautyEstiva.Desktop.Views.StaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBg" Color="#0f0a1e" />
        <SolidColorBrush x:Key="CardBorder" Color="#1a1030" />
        <SolidColorBrush x:Key="TextDim" Color="#5a4d7a" />
    </UserControl.Resources>

    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{DynamicResource Staff}" FontSize="24" FontWeight="Bold"
                   Foreground="White" Margin="0,0,0,20" />

        <!-- Loading -->
        <ProgressBar Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}"
                     IsIndeterminate="True" Value="0"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" />

        <!-- Data Table -->
        <Border Grid.Row="1" Background="{StaticResource CardBg}" BorderBrush="{StaticResource CardBorder}"
                BorderThickness="1" CornerRadius="16">
            <DataGrid ItemsSource="{Binding StaffMembers}" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column"
                      Background="Transparent" Foreground="White" BorderThickness="0"
                      RowBackground="#0f0a1e" AlternatingRowBackground="#110c20"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#1a1030" FontSize="13">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#0a0815" />
                        <Setter Property="Foreground" Value="#5a4d7a" />
                        <Setter Property="FontSize" Value="11" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="Padding" Value="16,12" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="BorderBrush" Value="#1a1030" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="16,10" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#1a1030" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="AD SOYAD" Binding="{Binding FullName}" Width="*" />
                    <DataGridTextColumn Header="E-POSTA" Binding="{Binding Email}" Width="200" />
                    <DataGridTextColumn Header="TELEFON" Binding="{Binding Phone}" Width="140" />
                    <DataGridTextColumn Header="ROLLER" Binding="{Binding RolesDisplay}" Width="140" />
                    <DataGridTemplateColumn Header="DURUM" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="6" Padding="8,4" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                    <Setter Property="Background" Value="#052e16" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Background" Value="#1c1917" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Text" Value="Aktif" />
                                                        <Setter Property="Foreground" Value="#10b981" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Text" Value="Pasif" />
                                                        <Setter Property="Foreground" Value="#6b7280" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Pagination -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button Content="{DynamicResource Previous}" Command="{Binding PreviousPageCommand}"
                    Foreground="#8a7cb0" Background="Transparent" BorderBrush="#1a1030"
                    BorderThickness="1" Padding="16,8" Margin="0,0,8,0" Cursor="Hand" />
            <TextBlock VerticalAlignment="Center" Margin="8,0" Foreground="#5a4d7a" FontSize="13">
                <Run Text="{Binding CurrentPage}" /><Run Text=" / " /><Run Text="{Binding TotalPages}" />
            </TextBlock>
            <Button Content="{DynamicResource Next}" Command="{Binding NextPageCommand}"
                    Foreground="#8a7cb0" Background="Transparent" BorderBrush="#1a1030"
                    BorderThickness="1" Padding="16,8" Margin="8,0,0,0" Cursor="Hand" />
        </StackPanel>
    </Grid>
</UserControl>
